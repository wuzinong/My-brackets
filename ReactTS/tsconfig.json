{
    "compilerOptions": {
        "outDir":"./dist/", // 构建输出目录 (因为我们大部分时间都在使用 Webpack，所以不太相关)
        "sourceMap": true,// 使 TypeScript 生成 sourcemaps
        "noImplicitAny": true,
        "allowSyntheticDefaultImports":true,//允许使用import React from 'react'这种方式引入模块
        "module":"commonjs", // esnext支持 lazyload
        "target": "es5", // 编译成 es5
        "jsx":"react",
        "removeComments": false,
        "lib":[
         "dom",
         "es6",
         "dom.iterable",
         "scripthost"   
        ],
        //"strict":true
        //"baseUrl": "src", // 可以相对这个目录 import 文件
        //more:https://basarat.gitbooks.io/typescript/docs/options/noImplicitAny.html  
    },
    "include": [
        "./src/**/*"
    ],
    "exclude": [
        "node_modules"// 这个目录下的代码不会被 typescript 处理
    ]
}